# Dependencies
node_modules/
.venv/
.pnp
.pnp.js

# Build outputs
.next/
out/
build/
dist/

# Environment — NEVER commit these
.env
.env.local
.env.*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# Claude Code local settings (machine-specific permissions, may contain secrets)
.claude/settings.local.json

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Drizzle generated migrations (optional — commit if you want reproducible migrations)
# drizzle/

# RAG datasets (50-200MB each — download via npm run rag:download)
scripts/datasets/
